# Use LF line endings for all text files (Linux/macOS convention, works on Windows too)
* text=auto eol=lf

# Binary files (don't normalize)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
